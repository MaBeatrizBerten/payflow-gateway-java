version: '3.8'

services:
  postgres:
    image: postgres:17-alpine
    container_name: payflow_db_local
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: payflow
      POSTGRES_PASSWORD: payflow
      POSTGRES_DB: payflow